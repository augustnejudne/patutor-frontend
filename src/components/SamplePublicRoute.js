import React from 'react';
import Layout from './Layout/Layout.js';

const SamplePublicRoute = ({ props }) => {
  return (
    <Layout>
      this is SamplePublicRoute
    </Layout>
  );
};

export default SamplePublicRoute;